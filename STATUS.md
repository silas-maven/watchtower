# Watchtower (SPA - Stock Pickers Academy) — Status

## Current State
Deployed at watchtower-virid.vercel.app (Next.js 16 + Prisma + Supabase `watchtower` schema + Clerk + Stripe). Member app (`/app/*`) and admin console (`/admin/*`) functional. Major pushes: Product v2 (2026-06-12/13), Spartan portfolio (2026-06-16), client-feedback round (2026-07-21). Typecheck clean, 57 unit tests pass.

## 2026-07-22 — Client feedback round two (pitch overhaul + inline plans + more)
Full implementation of the 2026-07-22 WhatsApp feedback (`docs/client-feedback/2026-07-22-whatsapp-forward/`). typecheck/lint clean, 62 tests, `next build` green. Migration `20260722000000_stock_requests` applied to the shared DB. Roundup: `docs/client-feedback/2026-07-22-whatsapp-forward/2026-07-22-implementation-roundup.md`; in-app release note added to `lib/releases.ts` (v2026.07.22).
- **Pitch overhaul (feedback 3/4/5/6):** `lib/ai/pitch.ts` rewritten. New `lib/market/yahoo.ts fetchYahooCompanyData` pulls business summary + analyst targets (targetMean/high/low, numberOfAnalysts, recommendationKey) + full health fundamentals (margins, ROE, FCF, cash/debt, EBITDA, growth) — all free from Yahoo financialData + assetProfile. `lib/finance/health.ts` = deterministic traffic-light scorer (profitability/growth/leverage/liquidity/cashGeneration/bankruptcyRisk, weighted, red/amber/green + 0-100 + tests). Prompt now enforces STRICT per-section responsibilities (Story = company + 12mo, Weather = macro ONLY, FinHealth interprets + scored, Key Risk company-specific). Section 9 "Trade Plan (Execution)" → **"Analyst Price Targets"** (blended target/upside or "no coverage"). Time Horizon fixed to **3-5 years**. Pitch type now carries structured `financialHealth` + `analystTargets`. **Live-verified on DIS** (the complained-about stock): Story explains Disney + down 23%/earnings -29.8%, FinHealth 53/amber with dims, analyst 30/mean 127/+32.8%.
- **Pitch modal (feedback 2/4):** `GeneratePitch.tsx` renders the health traffic-light + analyst target bar, an **animated loading state** (mk-pulse steps + mk-shimmer skeleton; new `.mk-shimmer` in globals.css), and **up/down section reorder** (copy follows order).
- **Inline averaging plan (feedback 1):** `components/portfolio/InlineAveragePlan.tsx` — holdings table row expands to view/create/edit the plan **inline, same page** (no nav to planner); reuses `computeAveragingPlan`, POSTs with `linkHoldingId` (auto-adds to watchlist on 409), refreshes via new `onRefresh` prop on `HoldingsTable`. Both live + virtual pages pass `onRefresh={load}`.
- **Asset actions (feedback 7):** `components/assets/AssetActions.tsx` on the asset detail page — Add to watchlist, Add to portfolio (live/virtual modal → existing `/api/me/portfolio/{live,virtual}` POST), Create plan (deep-link). Hidden for macro assets.
- **5Y + Max charts (feedback 8):** `RANGE_DAYS` + history route + asset Price history selector gain `5y`/`max` (max ≈ 40y; verified DIS→1986, JMIA→2019 IPO). Indicator view unchanged (uses timeframes).
- **SPA logo (feedback 9) — REAL logo now.** Kyser sent the brand artwork; `components/brand/SpaLogo.tsx` (`SpaEmblem`) is a faithful SVG recreation (green up-trend arrow via `#spaArrow` gradient over 8 gold-to-green candlestick bars via `#spaBars` gradient) replacing the "SPA" text chip in TopNav + MarketingNav/Footer. New `components/brand/SpaLogoLockup.tsx` = emblem + wordmark, **animated on the landing hero** (bars scaleY-rise staggered, arrow stroke-draw via pathLength=1, wordmark fade-up; all gated behind `prefers-reduced-motion: no-preference`, keyframes `spa-*` in globals.css). Wired into `app/page.tsx` hero. **Browser-verified** (dev server screenshot): renders faithfully. If the official vector file arrives, swap the artwork in `SpaEmblem`.
- **Subscription lifecycle (feedback 11):** Stripe webhook now creates a `subscription_ended` BillingAlert on `customer.subscription.deleted` / canceled / unpaid / cancel_at_period_end (never auto-cuts). Surfaces in the existing admin Members → Billing queue (lists all OPEN alerts) for manual removal via AdminAccessAction. Resolves on next payment.
- **Stock request flow (feedback 10):** new `StockRequest` model + migration; `/api/me/stock-requests` POST + `/api/admin/stock-requests` GET/PATCH; member `RequestStock` card on Asset Centre; admin `StockRequestsPanel` on /admin/assets (PENDING→REVIEWED/ADDED/DECLINED).
- **⚠ Decisions locked with Kyser:** everything (P0+P1+P2); SPA logo = recreate as SVG now (real file incoming); subscriptions = Stripe-flag + manual removal.
- **NOT browser-verified** (Clerk-gated): pitch modal visuals, inline plan expand/create, asset action buttons, 5Y/Max render, logo, stock-request round-trip. Deterministic/API layers verified by tests + live scripts (pitch, health, 5y/max fetch).
- **Deploy:** `vercel --prod` (CLI-only project). Still uncommitted until Kyser asks.

## 2026-07-21 — Macro data auto-feeds + in-app admin Release Notes (uncommitted)
Follow-up to the feedback round. Deployed `vercel --prod` (`watchtower-m9yguhyjn`); smoke green (cron 200, ticker 401, /admin/releases 404 unauth = normal Clerk protection). typecheck/lint/build clean, 57 tests.
- **BOE Base Rate now auto-fetches** (`lib/market/boe.ts`) from the free BoE IADB CSV (series `IUDBEDR`, no key), parsed for the latest dated value. Refresh job writes it to a new `macro_boe_base_rate_auto` platform setting; `getMacroTiles` uses the admin manual `macro_boe_base_rate` as an override when set, else the auto value. **Live-verified: 3.75% as of 21 Jul.** MacroSettingsPanel hint updated ("leave blank to use it; enter only to override").
- **Sector P/E now auto-populates** (`lib/market/sectorPE.ts`): no direct free source, so each stock is benchmarked against its **SPDR sector ETF** (XLK/XLF/XLV/… → trailing P/E, all free via Yahoo `quote`). `fetchYahooFundamentals` now also returns `sector` (added `summaryProfile` module); the fundamentals pass fetches the 11 sector-ETF P/Es once per run and writes `Asset.sectorPE` matched by sector (only when resolved, so a manual value is not wiped). **Caveat documented: US-sector benchmark** (a UK financial gets the US financials P/E). **Backfilled live: 10/13 active equities populated** (DIS 16.0, KO 25.7, NKE 29.2, LLOY 17.7…; 3 blanks = names with no Yahoo sector, correctly dashed).
- **UK 10Y Gilt + iTraxx 5Y stay manual** — no free source (iTraxx is licensed; a free HY-credit-spread substitute exists but is a different index, would be relabelled honestly). Both remain admin-set in Macro Readings.
- **In-app admin Release Notes** (`/admin/releases`, `lib/releases.ts`): "What's New" page (+ admin nav item + Overview card) listing shipped changes grouped by area, each item tagged with its feedback section and **deep-linking to where the change lives** (asset detail supports `?view=indicator|alerts` now; asset-specific links resolve against a live sample asset id server-side). Single source of truth is `lib/releases.ts`.
- **Client roundup doc updated** (`docs/client-feedback/2026-07-21-implementation-roundup.md` section 10 + new in-app-release note): BOE + sector P/E now automatic, gilt/iTraxx manual with the honest why.

## 2026-07-21 — Client feedback round (P0+P1+P2) BUILT + DEPLOYED (uncommitted)
Full implementation of the 2026-07-19 WhatsApp feedback (`docs/client-feedback/`), all slices, deployed `vercel --prod` (`watchtower-ic16bkl15`). Smoke test green: cron refresh 200, new APIs 401 auth-gated, landing 200. **Working tree NOT committed** (commit as account 2 when Kyser says). Plan of record: `~/.claude/plans/we-are-going-to-ethereal-puppy.md`.
- **Slice 0 — data foundation:** migration `20260720000000_client_feedback_foundation` APPLIED to the shared prod DB (additive): Asset gains `ma50/ma200/dividendYield/quickRatio/currentRatio/debtToEquity/sectorPE/fundamentalsAt/isMacro`, AssetType `REIT`, new `AiReport` (PITCH/STRESS_TEST/PERSONAL_FINANCE) + `PersonalFinanceInput` tables. Yahoo layer: quote mapping adds 50d/200d MA + dividend yield; `fetchYahooFundamentals` (quoteSummary financialData, D/E normalised /100) throttled to 5 stale equities per refresh run (24h staleness); `fetchYahooOhlc` (OHLC + 1d/1wk/1mo intervals + `full=1` for 200-period indicator warmup) behind `/api/assets/[id]/history`. **12 macro assets seeded** (`scripts/seed-macro-assets.ts`, idempotent; GBPUSD/EURUSD/SPX500/NASDAQ/FTSE100/GOLD/SILVER/OILWTI/NATGAS/BTCUSD/VIX/DXY with Yahoo quoteSymbols) — all pull real quotes; excluded from member lists via `isMacro: false` filters (assets page, /api/assets, watchlists, dashboard, signals). Manual macro settings `macro_boe_base_rate`/`macro_uk_10y_gilt`/`macro_itraxx_5y` ({value, changePct, asOf}) editable in **admin → Assets → "Macro Readings (manual)"** (`components/MacroSettingsPanel.tsx`); `lib/market/macro.ts getMacroTiles()` merges live+manual (client-safe half in `lib/market/macroTypes.ts`). FOREX/COMMODITY now count as open on weekdays in `isMarketOpenForAsset`.
- **Slice 1 — P0 amendments:** Key fields card expanded (Market Cap w/ course-table band labels via `lib/marketCap.ts` UK/US thresholds from feedback image 02; P/E Ratio rename; Sector P/E (admin-entered, dash until set); 50d/200d MA; quick/current/D-E ratios; next earnings; dividend yield; Stochastic (8,5,5) coloured red>=80/green<=20 computed client-side via `lib/indicators.ts`). Asset Centre table → client island `components/assets/AssetLibraryTable.tsx` with **Asset Class + Product columns** (`lib/assetClass.ts`) + **filters** (All/Buy/Sell/Both/Any-alert buttons, currency, market-cap band, search — shared `components/assets/AssetFilterBar.tsx`); same columns+filters on the watchlists master table. Master Watchlist fast-access chip (scrolls to section) + header link from Asset Centre. **Average Planner: per-tranche editable £ allocation + "Split evenly" button** (no more forced even split; schema already stored per-tranche budgetGBP). Nav renames per mockup: Command Centre→**Dashboard**, Asset Library→**Asset Centre**, Portfolio Tools→**Portfolio** (labels only, routes unchanged); mobile header now full "Stock Pickers Academy" brand. **Fixed the Price History y-axis garbage** (image 08): tickFormatter + auto domain.
- **Slice 2 — chart architecture:** new dep **lightweight-charts 5.2** (`components/charts/CandleChart.tsx`, theme-aware, candles/line + overlays + price lines + stochastics pane via v5 panes API). `lib/indicators.ts`: sma/bollinger(20,2)/stochastic(8,5,5) pure + tested. Asset detail now has a **view selector: Price history | Indicator view | Price alerts**. Indicator view (`components/assets/IndicatorView.tsx`): Daily/Weekly/Monthly (recomputes), toggles for candles/Bollinger/50d/200d (defaults on entry: Bollinger + stochastics, per feedback). Price alerts view (`components/assets/PriceAlertsView.tsx`): buy/sell alert cards + labelled blue/red chart lines + show/hide toggle, **multi-tranche buy-alert lines from the linked AveragePlan**, position summary (Live/Virtual/Both, weighted avg, unrealised P/L £+% via /api/market/fx), Spartan signal badge (BUY ZONE/WATCHLIST/SELL ZONE), Spartan tranche tracker w/ invested progress, last-alert-triggered card (SignalEvent). `/api/assets/[id]` now returns stats + signal + member position payload (holdings, plan+tranches, last event).
- **Slice 3 — ticker + Weather:** `components/market/TickerStrip.tsx` in the TopNav header (marquee via existing `.mk-ticker` CSS, 60s poll of `/api/market/ticker`, clickable live tiles → asset pages, grey static tiles for BOE/gilt/iTraxx). `lib/market/weather.ts` deterministic SUNNY/MIXED/STORMY/FROSTY classifier (documented thresholds, exact display copy from feedback) + tests. Dashboard: `components/market/WeatherSnapshotBoard.tsx` — Weather Outside panel + **Market Snapshot rows 1+2 with "View full market dashboard" revealing row 3** (mockup layout).
- **Slice 4 — pitch generator:** `lib/ai/pitch.ts` — 10-section Trade Idea Interview Checklist pitch; deterministic facts (signal-derived direction, targets, tranches, indicators, weather, fundamentals) narrated via `callJsonModel`, zod-validated, deterministic fallback. `/api/ai/pitch` persists `AiReport`. "Generate pitch" buttons: Asset Centre rows + asset detail Price alerts view; modal w/ copy + educational disclaimer. **Live-verified** (JMIA: BUY→BULLISH, real numbers, honest nulls).
- **Slice 5 — stress test:** `lib/stressTest.ts` seeded-PRNG Monte Carlo (2k paths, CAPM-from-beta assumptions documented as constants; goal probability, percentile range, max drawdown, concentration by stock/currency/cash/beta) + tests; CRO narration `lib/ai/stressNarrative.ts` (client's prompt framing, fallback included); `/api/ai/stress-test` + page `/app/portfolio-tools/stress-test` (Live/Virtual/Both + goal + horizon).
- **Slice 6 — personal finance:** `lib/finance/simulate.ts` deterministic MC over personal finances (salary growth/inflation/returns/expense shocks as documented constants; emergency months, saving rate, run-out risk, FI age, wealth-at-60 percentiles, optional structured goal) + tests; CFO narration `lib/ai/financeNarrative.ts`; `/api/ai/personal-finance` (saves inputs + persists AiReport) + `/api/me/personal-finance` (prefill) + page `/app/portfolio-tools/personal-finance`. Both new tools carry not-regulated-advice disclaimers + assumption footers.
- **⚠ Found in live test: the OpenRouter free model (`openai/gpt-oss-120b:free`) now 404s ("no longer free")** — every AI call fails over to OpenAI (gpt-5-nano) instantly, so nothing breaks, but daily brief/digest in prod are now effectively OpenAI-only. Either fund OpenRouter, pick a new free slug for `OPENROUTER_MODEL`, or drop the OpenRouter leg.
- **NOT browser-verified** (Clerk-gated): chart rendering (lightweight-charts), ticker marquee, Weather panel, filters, planner allocations, both P2 tools — needs Kyser's eyeball in prod. Deterministic/API layers all verified by tests + live scripts.
- **Open client questions** (tracked in the plan): pitch depth (90-sec built; full research pack?), snapshot row-3 grouping, sector P/E + gilt/iTraxx data sources (admin-set for now; BOE IADB CSV `IUDBEDR` is free if we want automation; FRED `BAMLH0A0HYM2` HY-OAS is a free iTraxx proxy).

## 2026-06-16 — Spartan Portfolio build SHIPPED (Slices 2-5) + DEPLOYED
Full Spartan-strategy portfolio built, committed (account 2), and deployed to prod (`vercel --prod` → `watchtower-pypat5nvt`). Commits: `fe23f61` (S2) · `d87710c` (S3) · `f8193ca` (S4) · `172ecb6` (S5). Migration applied to prod; smoke test green (cron 200, new APIs 401 auth-gated, `/app/*` 404 unauthenticated = normal Clerk protection).
- **Slice 2 — backbone:** migration `20260616000000_spartan_strategy` (ADDITIVE, `@@map` only / not multiSchema): `AveragePlanTranche` child table (orderIndex, price, budgetGBP, executed/executedAt), `AveragePlan.basePrice/targetSellPrice`, `UserHolding.{spartanEnabled default true, averagePlanId @unique → SetNull, manualNextBuyPrice, manualSellTarget}`. `lib/spartan.ts` (nextBuyPrice / sellTarget / impliedDropPct[from Trade 1] / weightPct / syncHoldingFromExecutedTranches / effective* / toPlanLite) — separate from the signal engine. Loaders (`livePortfolio`/`virtualPortfolio`) expose nextBuyPrice/sellTarget/spartanEnabled/hasPlan. APIs: `/api/me/average-plans` (GET, POST upsert + watchlist guard), `[id]` (GET, DELETE), `[id]/tranches/[trancheId]` (PATCH executed → sync linked holding), `/api/me/holdings/[id]` (PATCH spartan/manual/link), `/api/market/fx` (member-readable rates).
- **Slice 3 — Average Planner rebuild** (`average-calculator/page.tsx`): stock search + On Watchlist / Add to Watchlist; current price; Target Price to Sell; **price-based tranches** + add/remove + implied drop % vs Trade 1; per-tranche Executed; live preview (computeAveragingPlan, budget GBP split evenly, prices in stock currency); Final Position (all executed) + Actual (executed only); persists via POST; prefills existing plan on select; deep-links `?assetId=`.
- **Slice 4 — holdings columns + Spartan toggle:** shared `components/portfolio/HoldingsTable` (Next Buy, Sell Target, Avg Plan View, Spartan toggle, Status = Plan active / amber Create Average Plan). Spartan on → derived from plan (read-only); off → inline manual edit. Both live + virtual pages use it (parity). Wired to `/api/me/holdings/[id]`.
- **Slice 5 — allocation donut:** `components/portfolio/AllocationDonut` (recharts) — holdings + cash slice, total in centre, legend ticker + %; in both pages. Per-stock "detailed report" = the existing asset-detail pages via clickable tickers.
- **NOT browser-verified** (Clerk-gated): the end-to-end UI flows (create plan → link → toggle Spartan → Next Buy/Sell populate → tranche execute → holding sync → donut) need an eyeball by adding a holding + a plan. Build/tsc/lint/migration/route-deploy all verified.
- Budget decision: planner budget is **GBP** (prices in stock currency); deviates from the mockup's "Total Budget (USD)" but matches the GBP-centric portfolio + data model.

## 2026-06-16 — Owner feedback: mock purge + Slice 1 (Spartan goal set)
Owner tested live + sent direction (`feedback/2026-06-16-watchtower-feedback-capture/`): evolve Portfolio Tools into a Spartan-strategy portfolio (Holding → Averaging Plan → Spartan toggle). Goal + full plan: `~/.claude/plans/make-a-plan-set-recursive-karp.md`. This run = mock purge + Slice 1 + the CI 403 (403 fix pending the Vercel secret value).
- **Mock purge (full clean slate):** `prisma/seed.ts` reduced to the real asset import only (removed 7 `@watchtower.demo` profiles + sample watchlists + usage fixtures); deleted `app/login` + `app/api/auth/login` (demo password bypass; `/api/auth/login` was already a 410 stub) + `app/proof`; removed the `mockEmailLog` write in `lib/subscriptions/overdue.ts` (real `BillingAlert` kept); dropped `/login` from `TopNav` auth-page check. **Prod DB cleaned** via `scripts/cleanup-demo-data.ts` (deleted 7 demo profiles, dependents cascaded, 0 remaining) — admin Members panel is now real-only. KEPT: academy asset import, `formulaParity` (used by admin/assets + its API, NOT only proof), deterministic AI fallbacks. NOTE: `refreshMarket`/`settings` `portfolio_size_gbp` £5k left (internal formula-parity calc param, not user-facing mock).
- **£5k defaults → self-input:** removed `DEFAULT_VIRTUAL_SIZE_GBP`; virtual portfolio auto-creates with null starting value (falls back to invested cost like live); both views prompt to set "Portfolio Starting Value (Cash)".
- **Slice 1 (no schema change):** renamed Declared Size/Portfolio Size → **Portfolio Starting Value (Cash)** (live + virtual + dashboard); headers AVG→Avg Price, PRICE→Current Price; surfaced **Portfolio Beta** (loaders now pass real `snap.beta ?? asset.beta` into `computePortfolioSummary`, was `null`) + a Beta stat card; added **Weight %** column (value / (holdings value + cash)); asset cells now link to `/assets/[id]`.
- Verified: `tsc` clean, `eslint` clean, `next build` green (`/login` + `/proof` gone). Committed as account 2.

## 2026-06-14 — Watchlists custom modal + SSR perf + CI secret
- **CI fix (no code):** the `market-refresh` GitHub Action was failing with "CRON_SECRET repo secret is not set". Root cause: `CRON_SECRET` was set as a **Vercel env var** but the workflow reads `${{ secrets.CRON_SECRET }}` — a **GitHub Actions repo secret** (separate store). Set the repo secret on `silas-maven/watchtower` from local `.env` (value matches Vercel) via `gh secret set` as account 2. Next run passes.
- **Custom modal:** new `components/ui/Modal.tsx` — themed portal dialog (dark card / gold primary, Escape + backdrop-click close, body-scroll lock). Replaced BOTH native browser dialogs on the watchlists page: `window.prompt` (new list) → input modal; `window.confirm` (delete) → confirm modal.
- **Watchlists SSR (perf):** the page was the only client-side fetch waterfall — painted empty, then fired two `no-store` API calls on mount, each re-running Clerk auth + `getDefaultWatchlist` + the main query (double-auth + "Loading…" flash = the "lists slow to load"). Now server-rendered: new `lib/server/watchlists.ts` (`getWatchlistsPageData` = one auth upstream, then assets + lists in parallel), `app/app/watchlists/page.tsx` is a server component feeding a thin client island `components/watchlists/WatchlistsClient.tsx`. ~6 DB roundtrips + 2 HTTP → 2 parallel queries, data on first paint. Master list now reflects owner overrides (`effectiveSignalState`) and triggers `ensureFreshMarketData` like other data pages.
- **Perf diagnosis (rest):** DB is pooled + co-located (`pooler.supabase.com:6543`, `pgbouncer=true`, eu-central ↔ `fra1` functions) — not the bottleneck; all other pages already server components. Remaining prod slowness is most likely serverless cold starts. Open follow-ups (not done): convert the other 3 client-waterfall tool pages (live/virtual/average portfolios), add `loading.tsx` skeletons, keep-warm ping.
- Verified: typecheck ✓, lint ✓, `next build` ✓ (`/app/watchlists` now ƒ dynamic SSR). Not browser-tested (Clerk-gated). Needs a deploy to reach prod.

### Admin/news polish (same day, second batch)
- **X timeline card fixed:** `data-theme` now follows the app's resolved theme (`useTheme`) and the embed rebuilds on light/dark toggle (the widget bakes the theme at load) — fixes the washed-out text in light mode. Removed the card's own `overflow-y-auto` so the iframe is the single scroll region (was a double scrollbar). `components/news/XTimelineCard.tsx`.
- **Members table actions de-cluttered:** replaced the 4-button colour rainbow with one inline primary action (Mark paid on overdue rows, else Review) + a portal-positioned `⋯` menu for the rest (Pause/Activate, Remove, Flag overdue, Review). New `components/ui/RowActionsMenu.tsx`. Tradeoff: secondary actions are one extra click.
- **Billing dates tidied:** members table `Due 2026-06-02 · Stage 1` → `Due 2 Jun 2026` + a separate red `Stage N` chip (en-GB); Billing + Audit tab timestamps → `12 Jun 2026, 14:30`, date moved to the top-right of each billing card. `components/UserManagementPanel.tsx`, `components/MembersHub.tsx`.
- **Admin accounts (how-to, no code):** roles come from env allowlists in `lib/auth.ts` — `SPA_ADMIN_EMAIL_ALLOWLIST` / `SPA_OWNER_EMAIL_ALLOWLIST`, comma-separated, trimmed + lowercased. Both vars exist in Vercel Production (currently just `hngpt52@gmail.com`). Add an admin = append their email to `SPA_ADMIN_EMAIL_ALLOWLIST` and redeploy; promotion only applies at first sign-in / profile creation (an existing member with a profile is NOT re-promoted by the allowlist — needs a DB role flip). Optional future: an owner-only "Make admin" toggle in the Members UI to avoid env edits + cover existing members.
- Verified: typecheck ✓, lint ✓, `next build` ✓.

### Favicon + color-scheme (third batch, same day)
- **Favicon rebranded:** replaced the off-brand blue watchtower `app/icon.svg` with a gold "SPA" chip matching the nav logo (`#ca8a04` ground, white "SPA"); deleted the stale blue `app/favicon.ico` so the SVG is the single source of truth.
- **`color-scheme` added** to `app/globals.css` (`:root` → light, `.dark` → dark). Fixes the X timeline rendering white in dark mode until hover (the UA was painting the cross-origin iframe's base canvas light); also makes native scrollbars / dropdowns / form controls render per-theme across the app. X dark-mode fix not browser-verified (Clerk-gated + external widget) but `color-scheme` is the standard cause/fix.
- Verified: `next build` ✓.

## Product v2 build (2026-06-13) — what shipped this session
- **Market data engine (Phase 1):** replaced the dead Yahoo v7 HTTP path with `yahoo-finance2` (`lib/market/yahoo.ts`), symbol mapping `LON:VLS`→`VLS.L` (`lib/market/symbols.ts`, per-asset `quoteSymbol` override), CoinGecko crypto fallback, per-asset `fetchError` capture + `JobRun` audit, currency-mismatch guard, market-hours gating (`lib/time.ts`), on-view freshness (`lib/server/marketFreshness.ts`), GitHub Actions scheduler (`.github/workflows/market-refresh.yml`, every 10 min in hours). VERIFIED LIVE: 14/19 assets pull real quotes; the 5 failures are delisted tickers (TOOP/CWBR/HZD/GNUS/CPE) correctly flagged.
- **Summaries (Phase 2):** AI brief now captures + logs `generationError` (the silent-failure bug is fixed; root cause = blank `OPENAI_API_KEY`). Deterministic per-member brief (`lib/server/memberBrief.ts`) on Daily Checks. Owner weekly digest (`lib/ai/weeklyDigest.ts`) + cron (Mon 07:00) + regenerate. Admin controls (`/admin/ai-briefs` toggles via `PlatformSetting`, `lib/server/settings.ts`).
- **Manual signal override (Phase 3a):** owner forces/suppresses BUY/SELL per asset (`effectiveSignalState` in engine, `/api/admin/assets/[id]/override`, "Owner call" badge), emits manual SignalEvent, AdminAssetAction audit. Asset CRUD completed: dark-themed `AssetCatalogManager`, `quoteSymbol`, verify-symbol endpoint, archive/delete.
- **Admin member intelligence (Phase 3b):** `lib/server/memberIntelligence.ts` (declared portfolios, holdings value, most-held/most-watched, recent closed positions). `/admin/members` restructured into tabbed hub (Overview | Members | Billing | Audit) via `MembersHub`.
- **Stripe (Phase 3c):** checkout takes `{ product: membership | ecourse }` (sub vs payment mode), `checkout.session.completed` handled, billing portal route, member subscribe/manage UI (`BillingPanel` on `/app/account`). No-auto-cut rule preserved.
- **Member sublists + Virtual Portfolio (Phase 4):** members CRUD multiple personal lists from the read-only master list (`/api/me/watchlists*`, rebuilt `/app/watchlists`). Virtual Portfolio paper-trading tool (`/app/portfolio-tools/virtual-portfolio`, `lib/server/virtualPortfolio.ts`). Blotter parity engine `lib/portfolio.ts` (summary strip, 4-tranche averaging w/ halving + multi-currency) + `profitGBP`/`priceVsYearHighPct` in `lib/formulas.ts`. 12 new unit tests.
- **Alert delivery (Phase 8):** INERT skeleton only (`lib/alerts/dispatch.ts`), gated behind `alert_delivery_enabled` (default false), in-app Notifications + stubbed channels. Not live, not documented for users — pending owner decision.
- **Schema:** additive migration `20260612030000_product_v2_foundation` applied to prod (SignalOverride, PortfolioKind, PlatformSetting, WeeklyDigest, JobRun, virtual-portfolio fields, quoteSymbol, fetchError, generationError, 4th averaging tranche). Did NOT drop the stray `dossier_visitors` table (belongs to another project, 2 rows).
- **AI providers (2026-06-13):** shared caller `lib/ai/llm.ts` tries OpenRouter first (`openai/gpt-oss-120b:free`) then OpenAI (`gpt-5-nano`) as fallback, both via Chat Completions JSON mode. dailyBrief/weeklyDigest/ai-insight all migrated onto it. 120s OpenRouter timeout → fails over to OpenAI on hang; `maxDuration` 300/180 on all AI routes (free model latency is ~160s). Em/en dashes stripped from model output (house style). VERIFIED LIVE: real brief persisted via gpt-oss-120b (`isFallback:false`), accurate movers, dash-clean. `OPENAI_API_KEY` no longer mandatory in prod env check (either provider satisfies). Note: dropped strict json_schema (OpenAI Responses-only) for json_object mode for cross-provider compat; server-side validation makes this safe.
- **Finance news (Phase 5):** `lib/news/aggregator.ts` aggregates 4 free RSS feeds (BBC Business, CNBC, MarketWatch, Cointelegraph) with entity decoding + dash normalisation + dedupe; `/api/news` (member-gated); `NewsFeedCard` (auto-refresh 10 min) + `XTimelineCard` (official embed, 5s fallback) on the member dashboard "Market Pulse" row. Admin-configurable via `news_feed_urls` / `news_x_handle` settings. VERIFIED: 40 clean items in 3.5s.
- **Marketing pages (Phase 6):** rebuilt landing (`app/page.tsx`) + new `app/pricing` to advanced standard. WebGL hero (`components/marketing/HeroCanvas.tsx`: gold wireframe market terrain, three + @react-three/fiber) loaded `ssr:false` behind content (LCP-safe), reduced-motion fallback via `useSyncExternalStore`, tab-hidden pause, DPR cap 2. GSAP ScrollTrigger reveals (`Reveal.tsx`). Shared `MarketingNav`/`MarketingFooter` (not-financial-advice disclaimer). One `TopNav` line so `/pricing` bypasses the app shell. Copy: UK English, no em/en dashes, no banned words, no return promises.
- **NOTE on subagents:** the two background subagents originally spawned for news + marketing were orphaned at a session-context boundary (did not survive) and produced nothing; both phases were rebuilt directly in the main session.

## Feedback round (2026-06-13, post-review) — all 8 items addressed
- **Sign-in routing:** modal sign-in/up now carry `forceRedirectUrl="/app"` (was leaving users on the landing page).
- **Mock data purged:** deleted dead `lib/mock.ts`; non-admin dashboard now values real holdings (`lib/server/livePortfolio.ts`) or shows an empty state, not a hardcoded £5,000 (removed `getPortfolioSummary`).
- **Live data on asset pages:** archived 5 dead/broken tickers (CWBR/GNUS/HZD/TOOP/CPE — CPE was the "0 MXN" stale Mexican listing); Asset Library reskinned dark with 14 live assets; asset detail rebuilt with a real Yahoo historical chart (`fetchYahooChart`, `/api/assets/[id]/history`, range selector) replacing the empty sparse one.
- **Live Portfolio:** new real-holdings view + API (`/app/portfolio-tools/live-portfolio`, `/api/me/portfolio/live`), the missing blotter "Live Portfolio" tab.
- **Performance:** `getAssetsForDashboard` cut from 30 snapshots/asset to 1 with a projected select (was building an unrendered sparkline); asset-detail API trimmed to 1 snapshot; watchlist-add de-duplicated + usage tracking made fire-and-forget. (Some of the observed lag was `next dev` per-route compile, gone in build.)
- **News rail:** `MarketPulseRail` — news + X moved to a collapsible sticky right rail on the Command Centre (collapse persists to localStorage); Portfolio Tools tidied into a card row.
- **Members table:** split mislabeled "Portfolio Signal" into aligned Declared/Avg columns; actions made contextual (3-4 relevant buttons, not 6 always-on).
- **Base currency (app-wide):** `Profile.baseCurrency` (migration `20260613040000_profile_base_currency`), `lib/money.ts` + `lib/server/displayCurrency.ts`, Account setting (`BaseCurrencySelect` + `/api/me/profile`), Average Planner currency dropdown (defaults to base), and GBP→base conversion on the dashboard cards, Live Portfolio and Virtual Portfolio displays.
- Blotter tab mapping (answer to owner): Virtual Portfolio / Average Planner / Trade Journal (Closed Positions) / Due Diligence all present; Live Portfolio was the gap, now built.

## Verification (2026-06-13, full)
- `npm run typecheck`: clean. `npm run lint`: 0 errors (7 pre-existing warnings). `npm run test`: 26/26 pass. `npm run build`: success, all routes compile.
- Live-DB verified: market refresh (14/19 real quotes), member brief, daily brief + weekly digest via OpenRouter gpt-oss-120b (dash-clean), member intelligence, news aggregator.

## Goal (set 2026-06-12, scoping decisions locked with Kyser)
Take SPA from POC-grade to a production subscription product:
1. **Admin command of the platform**: aggregated view of member portfolios/trading activity; full CRUD control of the master asset list (all asset types); **manual signal override** (owner can force/suppress BUY/SELL regardless of calculation, clearly badged) on top of the existing owner-set target entry/exit levels.
2. **Member sublists** (DECIDED): master list is admin-controlled and read-only for members; members create their own multiple personal sublists of securities to track from it. No admin-curated themed lists for now.
3. **Real live market data** (DECIDED: 1-5 min polling in market hours via external free scheduler hitting the CRON_SECRET-guarded refresh endpoint). Current Yahoo path is dead, needs provider replacement + abstraction. Implement remaining Portfolio Blotter workbook tabs as user-facing tools (Virtual Portfolio missing; average calculator / closed positions / due diligence exist).
4. **Finance news card** (DECIDED: BOTH free news APIs/RSS aggregation AND an official X embedded timeline).
5. **Summaries**: fix AI brief generation in prod (currently 100% deterministic fallback, errors swallowed); add member-personalised summary (their sublists + portfolio) and owner weekly digest, with admin controls over these functions.
6. **User management**: layout cleanup; complete Stripe (checkout.session.completed handler, membership vs eCourse product selection); keep the rule "payment failure alerts admin, never auto-cuts access".
7. **Marketing surfaces**: landing/pricing pages rebuilt to advanced standard (Three.js/WebGL/GSAP, premium fintech aesthetic, artistic direction delegated to Claude). Note: no pricing page exists yet.

### Build order
Phase 1 market data engine (everything depends on real prices) → Phase 2 summaries (fix AI + personalised member brief + owner weekly + controls) → Phase 3 admin power (asset CRUD complete, manual override, member portfolio aggregation, user mgmt layout, Stripe completion) → Phase 4 member sublists + Virtual Portfolio tool → Phase 5 news card (RSS + X embed) → Phase 6 marketing pages.

## Session Log

### 2026-06-12
- Full codebase familiarisation + verification pass. Tests 14/14 pass, typecheck clean.
- **Found: market data pipeline has never worked in prod.** All 2,128 asset snapshots since the 12 May spreadsheet import are `source: fallback-previous` (zero successful Yahoo/CoinGecko fetches). Yahoo v7/v8 endpoints return 429 even with browser UA. Prices are frozen at import values; signal events stopped 15 May; the daily brief reports the same "8 buy signals" every day.
- **Found: AI brief never engages in prod.** All 111 daily briefs are `isFallback: true` (deterministic template). Cron + persistence layer works (brief generated daily 07:00 UTC). Cause unknown (env key missing on Vercel or API failure) because errors are swallowed silently in `lib/ai/dailyBrief.ts`.
- **Verified: user management wiring is correct.** Panel → role-gated PATCH `/api/admin/subscribers/:id` + mark-paid → AdminAccessAction audit trail. Stripe webhook maps payment failure → BillingAlert + OVERDUE mirror without touching accessState (matches owner requirement). Gaps: checkout only sells membership, no `checkout.session.completed`, waitlist is fake (signup = instant active member).
- DB is the shared Supabase instance; watchtower tables live in dedicated `watchtower` schema (DATABASE_URL has `?schema=watchtower`). All 3 migrations applied.
- vercel.json crons: refresh-market daily 00:00 UTC (README's "every 15 min" is stale), brief 07:00, overdue check 08:00.
- Created STATUS.md (this file). Repo not yet in ~/Desktop/jarvis/REGISTRY.md.

## Known Issues
- Market refresh fetches zero real quotes (Yahoo blocked). Needs provider replacement/abstraction (e.g. yahoo-finance2 npm with crumb handling, plus fallback chain) and a faster-than-daily trigger (Vercel cron is daily; external scheduler or on-demand refresh needed for "live").
- OpenAI brief generation silently fails in prod; no error logging.
- `app/api/cron/refresh-market` etc. guarded by CRON_SECRET (fine), but refresh loops assets serially with no rate limiting or per-asset error capture.
- Legacy POC models (User/Session/Subscription/PersonalWatch) still in schema.
- `.git/config` remote URL embeds a GitHub token (silas-maven account).
- README cron section stale; demo-credential section stale (Clerk replaced password auth).

## Next Steps
1. Kyser to answer scoping questions (sublists ownership, "pricing control" meaning, live-data cadence, news source).
2. Design + build in this order: market data fix (everything depends on real prices) → admin asset/levels/sublists control → member portfolio aggregation for admin → summaries (fix AI, personalise, weekly owner digest + controls) → Stripe completion + user management layout → news feed card → marketing pages overhaul.
3. Add watchtower to REGISTRY.md.
